import { CacheType, ChatInputCommandInteraction } from 'discord.js';
import { Match } from './types';

export function 

export function select(interaction: ChatInputCommandInteraction<CacheType>, match: Match, operatorList: string[]) {

}
